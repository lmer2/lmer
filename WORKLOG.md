# Worklog

## 2026-08-19 — MR !235 review iteration 2

- Published agents/global's audited pre-commit reuse opt-in in work-repo project
  info and pinned its exact host/project lookup path in the gate tests.
- Added the staged-added path projection used by `check-added-large-files` to
  the reusable pass key while preserving landing-train reuse for modifications.
- Kept Pi slash commands as keystrokes without guessing that Claude's visible
  chat-defusal prefix is inert in Pi, and moved release prose into the fragment.

## 2026-08-19 — MR !235 review iteration 1

- Narrowed assistant consistency to a register/remove-to-state publication lock;
  status now stays responsive during process spawn and termination waits.
- Reported the session-resolved harness back to the platform and preserved valid
  geometry at the minimum row count.
- Reworked pre-commit reuse around checked worktree bytes, external authorization,
  and the effective Git attributes/merge state used by the audited pinned hooks.
- Kept Claude control commands out of bracketed-paste framing while retaining the
  measured prose path, and folded the transcript diagnostics requested in review.

## 2026-08-19 — Issue #318 remaining #314 ledger

- Serialized assistant status snapshots with supervision starts so a published
  process cannot appear under the previous state generation.
- Reserved the last PTY row for Codex/Pi footer UIs while preserving full xterm
  geometry for Claude and other harnesses.
- Removed review-wrapper command inference from the signal guard; wrappers own
  their post-success milestone signal.
- Added default-off, operator-authorized, short-lived full pre-commit pass reuse;
  its landing-train-stable key covers exact checked bytes plus the bounded Git
  state used by this project's pinned hooks.
- Confirmed item 5 is fully covered by open MR !225 and left its danger-zone
  settings/runner implementation out of this batch to avoid duplication.
- Framed multi-kilobyte prose for the probed Claude/Codex/Pi TUIs while keeping
  Claude's column-one commands as executable keystrokes, and covered one 6.6 KB
  fenced payload from composer through transcript/chat settlement.
- Made native transcript adapters outrank canonical derived twins, with
  warn-once diagnostics and separate vocabulary/cosmetic-label metadata.

## 2026-08-19 — Issue #314 bounded ready slice

- Made GitLab and GitHub post-review wrappers report their own successful milestones.
- Followed explicit `reslugged_from` history in platform run resolution, answer, and resume.
- Removed the manual supervision restart test's registry/state persistence race.
- Added a content-checked gate-cache handoff from pre-commit staged state to the clean post-commit tree, with structured cache verdicts in receipts.
- Kept live-probe and product-decision items deferred on #314 for a later slice.

## 2026-08-19 — MR !230 review iteration 1

- Made post-review milestone delivery best-effort so a posted review always
  returns the reviewer command's successful status.
- Recorded a passing test result before the optional commit-handoff index probe,
  and limited that mutating Git probe to cache-enabled `gate-commit` runs.
- Indexed live reslug successors once per mirror revision and reused one shared
  `reslugged_from` predicate in platform and work-repo consumers.
- Kept the pre-existing tracked-key/adoption split deferred to #263 because it
  needs a product/data-model decision rather than an implicit alias change.

## 2026-08-19 — issue #313: Codex harness improvements

- Reproduced the ambient `LMER_HARNESS=codex` supervisor-default failure and
  established the untouched baseline with `env -u LMER_HARNESS gate-check`.
- Added an image-managed Codex Stop hook that turns answered `lmer-ask`
  questions into native continuation turns without exposing answer text.
- Added Codex bracketed-paste submission framing and control-plane `/followup`
  translation while preserving original-payload delivery receipts.
- Isolated the supervisor test module from ambient lmer session state and added
  hook, image, PTY, FastAPI, and harness-regression coverage.
- Review iteration 1 replaced the removed `/prompts:followup` mechanism with a
  plain-text hook instruction, made the ask guard select any oldest unread
  answer, pinned Codex hooks on, removed duplicate unreleased changelog edits,
  and hardened bracketed paste against an embedded terminator.
- Review iteration 2 rebased onto `prep-release` while retaining both worklog
  sections, corrected the last two live Codex slash-command claims, and pinned
  the wind-down verb through Codex's framed supervisor submit path.
