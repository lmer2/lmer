changed:
- "Git authentication for lmer-created clones now uses mode-0600 container credential files, keeping tokens out of clone arguments, remotes, and repository config while leaving operator-owned bind-mounted checkouts untouched."
