security:
- "gate-push output no longer prints credentials embedded in remote URLs (#281)"
- "The generic GITLAB_TOKEN fallback is scoped to its issuing host (new LMER_GITLAB_TOKEN_HOST, default: the work-repo host from LMER_WORK_REPO or the platform config); third-party hosts no longer receive it (#161)"
- "Work-repo run-state writers now redact secrets; redaction covers GitHub/Slack/GitLab-runner token shapes and URL-embedded credentials; git error output is credential-scrubbed (#124)"
