fixed:
- "Fan-out selection is no longer ambient in session containers: forwarded as LMER_SPAWN_AGENTS/_CONFIG and read only by spawn-harness (#283)"
- "claude-runner now really merges settings.local.json and .mcp.local.json overlays; both jq filters failed silently before (#251)"
- "claude-runner startup no longer deletes concurrently-starting runners' prompt files (#276)"
- "The work goal fallback file is scoped per session, so test runs cannot clobber a live session's goal (#277)"
- "Closing a browser tab on a terminal view no longer dumps an ASGI traceback (#247)"
- "Harness task-notification blocks no longer render as operator turns in the assistant chat pane (#242)"
- "Returning to a terminal view no longer sends a redundant resize to the shared PTY (#218)"
- "Release leg 2 wires LMER_RELEASE_GITHUB_TOKEN into the GitHub mirror remote, so the mirror push works (#264)"
changed:
- "docs/RELEASE-PROD-SETUP.md is a reusable checklist again; deployment receipts belong in the release run's work-repo records (#266)"
- "The self-dev taskdef states measured gate provenance: bare gate commands run the operational copy (#282)"
